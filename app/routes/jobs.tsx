// Placeholder. T14 replaces this with the real jobs list (B17): status filter,
// "New job" dialog, and the per-job detail route.
export default function JobsRoute() {
  return (
    <div className="p-6">
      <h1 className="text-lg font-semibold tracking-tight">Jobs</h1>
    </div>
  );
}
